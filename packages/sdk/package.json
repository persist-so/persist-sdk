{
  "name": "@persist-so-npm/sdk",
  "version": "1.0.0",
  "description": "The official Node.js SDK for Persist S3 & IPFS Storage",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "keywords": ["ipfs", "s3", "storage", "web3", "persist"],
  "author": "Persist <admin@persist.so>",
  "repository": {
    "type": "git",
    "url": "https://github.com/persist-so/persist-sdk.git",
    "directory": "packages/sdk"
  },
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.9.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "axios": "^1.16.1",
    "form-data": "^4.0.5"
  }
}
